def solving(ls):
    #start점을 정하자
    start = []
    stack = []
    visit = []
    for i in range(len(ls)):
        for j in range(len(ls)):
            if ls[i][0] != ls[j][1]:
              start.append(ls[i])





info = list(map(int, input().split()))
line =